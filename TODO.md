# TODO

- [x] Migrate alpha cache to local cache path (`~/.wqb_mcp/cache/`)
- [x] Skip fetch in `warmup.py` if local cache is still valid
- [x] Migrate to Python `logging` module (replace custom `print`-based logging)
